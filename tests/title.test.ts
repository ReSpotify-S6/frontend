import test, { expect } from "playwright/test";

test('Check if title is correct', async ({ page }) => {
  // await page.goto('http://localhost:3000');
  // const title = await page.title();
  // expect(title).toBe('ReSpotify');
});